/* eslint-disable no-constant-condition */
/* eslint-disable max-len */
import styled from '@emotion/styled'
import { useTheme } from '@mui/material'

export const Logo = () => {
  return <div>some svg logo</div>
}
